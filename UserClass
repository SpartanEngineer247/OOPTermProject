#pragma once

#ifndef USER_H
#define USER_H
#include<string>
#include "Users_Database.h"

using namespace std;

class User
{
private:
    string NetID;
    string password;
    bool status;
    double operationTime;

public:
    User(string inputNetID, string inputpassword)
    {
        NetID = inputNetID;
        password = inputpassword;
        status = false;
        operationTime = 0.0;
    }

    ~User()
    {
        NetID = "-1";
        password = "-1";
        status = false;
        operationTime = 0.0;
    }

    string getID()
    {
        return NetID;
    }

    string getPass()
    {
        return password;
    }

    bool getStatus()
    {
        return status;
    }

    double getOperations()
    {
        return operationTime;
    }

    bool checkPassword(static string passwordtry)
    {
        if (passwordtry == password)
        {
            return true;
        }
        else { return false; }
    }

    vector<bool> CheckWashingMachineStatus(string building)
    {

    }

    int BookWashingMachine(string Building, int Machine)
    {

    }

    int BookDryingMachine(string Building, int Machine)
    {

    }
};

#endif
