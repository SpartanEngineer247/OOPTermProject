
#include <chrono>
#include <iostream>
#include <future>
#include <time.h>
#include <thread>
using namespace std;

int main ()
{
    int s =10;
   auto a = std::async(timer,s);//(functionname,time in seconds)
    std::cout << "test\n";
     auto b=std::async(timer,20);
    //auto a1=   std::async(timer);
    // auto future110 = std::async(timer);
}
